const button = document.querySelector(".button");
const nav = document.querySelector(".nav");
const info = document.querySelector(".info");
const body = document.querySelector("body");

export { button, nav, info, body };
